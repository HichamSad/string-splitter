class TagManipulator():    
    def parse_string(self, tags):
        result = []

        # Split on comma
        #commaSepElems = tags.split(',', tags)

        if len(tags) < 1 :
            return result

        return result